# Codex adapter

Place the managed workflow block in the global `AGENTS.md` or the nearest repository `AGENTS.md`. Keep native Codex role files and the deterministic MochiCode controller as the execution engine. Use `codex debug models` during release audits, then test a candidate before changing the chosen model map.
