# Claude adapter

Place the managed workflow block in the global `CLAUDE.md` or the nearest project `CLAUDE.md`. Keep Claude-specific hooks, permissions, plugins, and model settings intact. Use Claude's own configured update channel for application updates, then run a representative canary before accepting a model-policy change.
