# Astra mode

Use this only when the running parent is `gpt-6-astra`, or when preparing an explicit Astra migration. Current session identity proves the running model; catalog omission does not disprove it. The installed Codex catalog must list that exact model before installing a new profile. If that probe omits Astra, report the installation gate as unmet while preserving any already selected Astra parent.

## Routing

- Keep substantial, coupled, visual, architectural, debugging, integration, and long sequential work direct in the selected Astra parent, preserving its selected effort. High is not required merely because Sol previously used it. Low/Medium are comparison candidates, not silently applied defaults. Astra does not automatically add Manager Mode.
- Consider higher effort for consequential decisions or verified reasoning difficulty, after ruling out missing tools, unclear acceptance, and environment failures. Use only efforts advertised by the active host. Ultra is exceptional but permitted when that host advertises it; host capabilities may differ from API documentation.
- Delegate only when two or more leaves are frozen, disjoint, independently verifiable, and likely to save critical-path time. State the exact worker count because Astra may otherwise delegate less than intended. Start with two. Keep Luna for repeatable implementation and Terra for bounded read-only support. Astra retains product decisions and final integration.
- Keep Manager Mode explicit and unpromoted: activate only for an explicit Manager Mode implementation request, not a mention in research or planning. If selected, Astra owns direction and final judgment while the existing Sol High implementer owns sequential phases under the no-descendants policy and verified host controls. Do not create an Astra manager plus Astra implementer by default.
- Keep the existing fresh Sol High verifier for consequential-risk triggers. The Astra parent adjudicates its evidence. Do not add a second Astra call until a matched comparison proves that it improves accepted quality enough to repay the cost.

## Follow-through and instruction hygiene

Infer routine gaps from repository evidence and continue reversible work. Ask only when missing input changes safety, authority, cost, irreversibility, or the product result. Complete authorized preparation before presenting a human gate.

Astra follows skills and `AGENTS.md` more literally than earlier models. Load the minimum relevant skills, name the exact instruction that forces a pause, and resolve contradictions in favor of the user's explicit request and higher-priority safety rules. Do not add extra workflow prose merely because Astra can follow longer prompts.

Use the existing plain-language style. Tone and anti-slop instructions should describe the desired voice directly, not accumulate lists of banned phrases. Preserve human judgment for visual taste and usefulness.

## Tools, reasoning, and verification

Read [Astra cost controls](../../../docs/ASTRA-COST-CONTROLS.md) only when the user raises cost or usage concerns.

- Calibrate tests to risk. Start with the smallest meaningful command, run required repository checks, and broaden only after a change, failure, weak oracle, or consequential boundary justifies it.
- Let Astra perform consequential browser or computer verification directly when those tools are available. Do not create a separate UI agent merely to exploit its computer-use strength.
- Async tool calls, mid-turn steering, and `configuration_update` reasoning changes are host or Responses API capabilities. Use them only when the active runtime advertises them. Never simulate them with background polling, config rewrites, or extra agents.
- When mid-turn steering is available, incorporate the new instruction into completed work and continue from the current state. Do not restart discovery or discard accepted work.
- When `configuration_update` is available, keep the cached prompt prefix stable and change effort only at a real difficulty boundary. This does not authorize changing the user's stored global model preference.

## Promotion evidence

Before automatically promoting Astra to the installed default, require a live catalog receipt and a matched task against the current direct Sol route. Quality must pass first. Record total and uncached input, output, reasoning, wall time, rework, defects, human preference for visual work, and whether delegation actually occurred. Keep the prior route recoverable until at least one second distinct task confirms the result.

Official sources: [Astra model guidance](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra), [Astra model card](https://developers.openai.com/api/docs/models/gpt-6-astra), and [Codex subagents](https://learn.chatgpt.com/docs/agent-configuration/subagents).
